#!/usr/bin/env python
# -*- coding:utf-8 -*-

name = " aleX"

# a.移除name 变量对应的值两边的空格，并输入移除后的内容
# v = name.strip()
# print(v)


# b.判断name 变量对应的值是否以"al"开头，并输出结果
# if name.startswith("al"):
#     print("name is started with 'al' ")
# else:
#     pass

# c.判断name 变量对应的值是否以"X"结尾，并输出结果
# if name.endswith("X"):
#     print("name is ended with 'X' ")
# else:
#     pass


# d.将name 变量对应的值中的“l”替换为“p”，并输出结果
# v = name.replace("l", "p")
# print(v)


# e.将name 变量对应的值根据“l”分割，并输出结果。
# v = name.split("l")
# print(v)

# f.请问，上一题e.分割之后得到值是什么类型（可选）？
# v = name.split("l")
# print(type(v))

# g.将name 变量对应的值变大写，并输出结果
# v = name.upper()
# print(v)

# h.将name 变量对应的值变小写，并输出结果
# v = name.casefold()
# print(v)

# i.请输出name 变量对应的值的第2 个字符？
# print(name[1])

# j.请输出name 变量对应的值的前3 个字符？
# print(name[:3])
# print(name[0:3])

# k.请输出name 变量对应的值的后2 个字符？
# print(name[-2:])


# l.请输出name 变量对应的值中“e”所在索引位置？
# print(name.find("e"))

# m.获取子序列，仅不包含最后一个字符。如：oldboy则获取oldbo;root则获取roo
# print(name.strip(name[-1]))

